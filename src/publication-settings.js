// Keep these sections unpublished until the user explicitly asks to restore them.
// Requested on 25 September 2026; applies to every language and screen size.
export const SHOW_BUSINESS_CONTACT_DETAILS = false;
export const HIDDEN_SERVICE_ROUTES = new Set(["/terms", "/shipping-returns", "/privacy"]);
